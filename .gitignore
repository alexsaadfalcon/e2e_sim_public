# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / tooling caches
.pytest_cache/
.coverage
htmlcov/

# Generated artifacts
e2e/main/figures/

# Large generated data (regenerate via scenario_runner / sionna scripts) -- never commit
e2e/environment/sionna_sims/
e2e/environment/sionna_frames/
e2e/ml/datasets/
*.pkl
# Frame dumps: SinkBlock writes .npz, and its webapp default lands INSIDE the tracked
# package dir. Both were reachable by a plain `git add -A` (report/ alone was 156 MB).
*.npz
webapp/_sink_output/

# Training/report artifacts: figures, metrics JSON, corpus statistics. CHANGELOG already
# stated these were untracked -- this is what actually makes that true.
report/

# Presentation assets (large, not part of the code deliverable)
slides/

# Internal working docs -- not part of the public release. The public docs
# (README, CHANGELOG, CONTRIBUTING, ROADMAP, tests/ and webapp/ READMEs) are tracked.
TODO.md
CLAUDE.md
# Internal dev tooling (swarm agent/skill definitions reference internal docs + local paths)
.claude/

# Private orchestration notes (its own git repo, never public)
notes/

# Training runs: checkpoints (.pt, multi-MB each) and their history. Data, not source --
# same policy as e2e/ml/datasets/ above. Added 2026-08-25 after a cold-read of the
# handoff found runs/ was untracked only by luck: it was listed as never-commit in the
# standing rules while nothing enforced it, so one `git add e2e/` would have staged
# checkpoint binaries into a push-pending public history.
e2e/ml/runs/
*.pt
